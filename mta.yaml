_schema-version: 3.3.0
ID: firstcapproject
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: firstcapproject-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: firstcapproject-db
      - name: firstcapproject-auth
      - name: firstcapproject-destination

  - name: firstcapproject-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: firstcapproject-db

  - name: firstcapproject-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: firstcapproject-auth
      - name: srv-api
      - name: firstcapproject-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    build-parameters:
      build-result: app/
      requires:
        - name: firstcapprojectempmanageapp
          artifacts:
            - empmanageapp.zip
          target-path: app/

  - name: firstcapprojectempmanageapp
    type: html5
    path: app/empmanageapp
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: firstcapproject-destinations
    type: com.sap.application.content
    requires:
      - name: firstcapproject-auth
        parameters:
          service-key:
            name: firstcapproject-auth-key
      - name: firstcapproject-html5-repo-host
        parameters:
          service-key:
            name: firstcapproject-html5-repo-host-key
      - name: srv-api
      - name: firstcapproject-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: firstcapproject-html5-repository
              ServiceInstanceName: firstcapproject-html5-repo-host
              ServiceKeyName: firstcapproject-html5-repo-host-key
              sap.cloud.service: firstcapproject.service
            - Name: firstcapproject-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: firstcapproject-auth
              ServiceKeyName: firstcapproject-auth-key
              sap.cloud.service: firstcapproject.service

resources:
  - name: firstcapproject-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: firstcapproject-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: firstcapproject-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
        role-collections:
          - name: 'employee (firstcapproject ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.employee'
          - name: 'manager (firstcapproject ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.manager'
  - name: firstcapproject-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true

  - name: firstcapproject-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: firstcapproject-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
